@PACKAGE_INIT@

# If you want to enforce dependency discovery for consumers, you can do:
# include(CMakeFindDependencyMacro)
# find_dependency(GSL)
# find_dependency(HepMC3 QUIET)
# find_dependency(ROOT QUIET COMPONENTS Core RIO Hist Tree Graf Gpad)

# Import the exported targets (build-tree or install-tree)
# The calling find_package() will locate this file in either:
#  - <build>/lib/cmake/MARLEY/  (build-tree, generated by configure_package_config_file)
#  - <prefix>/lib/cmake/MARLEY/ (install-tree)
include("${CMAKE_CURRENT_LIST_DIR}/MARLEYTargets.cmake")

# You can expose convenience variables if helpful:
# set(MARLEY_DATA_DIR "@PACKAGE_SHARE_INSTALL_PREFIX@/marley/data")
